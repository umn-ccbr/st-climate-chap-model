[project]
name = "malaria-spatiotemporal"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "chapkit>=0.25.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
